/**
 * Saves a given score in localStorage if the given score would be in the top 3.
 * @param score score to add to localStorage.
 * @return {number[]} the top 3 scores in an array.
 */
export function saveScore(score) {
    return [];
}
